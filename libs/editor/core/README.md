# @kritzel/editor

Core editor package slot for the Kritzel editor UI.

This package is intentionally private until `kritzel-editor` and the editor UI components are extracted from `libs/engine/core`.
